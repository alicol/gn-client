import * as React from 'react';

export interface UserSignupProps {
    
}
 
const UserSignup: React.SFC<UserSignupProps> = () => {
    return ( <div>UserSignup</div> );
}
 
export default UserSignup;