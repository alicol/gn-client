import * as React from 'react';

export interface GameEditProps {
    
}
 
const GameEdit: React.SFC<GameEditProps> = () => {
    return ( <div>
        GameEdit
    </div> );
}
 
export default GameEdit;